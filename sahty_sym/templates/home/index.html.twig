{# templates/home/index.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sahty - Votre santé mentale au quotidien</title>

    <!-- CSS -->
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">

    <!-- Libs CSS -->
    <link href="{{ asset('lib/owlcarousel/assets/owl.carousel.min.css') }}" rel="stylesheet">
    <link href="{{ asset('lib/tempusdominus/css/tempusdominus-bootstrap-4.min.css') }}" rel="stylesheet">

    <!-- Icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Font Awesome 6 (for MEDINOVA icons) -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800;900&display=swap" rel="stylesheet">

    <!-- Animate.css -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>

    <style>
        /* ============ PALETTE DE COULEURS ============ */
        :root {
            --primary: #1D5D9B;
            --primary-light: #E8F4F8;
            --primary-dark: #163A5F;
            --secondary: #304D30;
            --secondary-light: #4A7C59;
            --accent: #75C2F6;
            --light: #F4F6FF;
            --dark: #1A2332;
            --success: #4A7C59;
            --danger: #C1666B;
            --warning: #D4A574;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif;
            background: var(--light);
            overflow-x: hidden;
        }

        /* Hero Section - Fusion des deux styles */
        .hero-section {
            background: linear-gradient(135deg, var(--primary-dark) 0%, var(--primary) 50%, var(--secondary) 100%);
            color: white;
            min-height: 90vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
            padding: 80px 0;
        }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 50%, rgba(117, 194, 246, 0.2) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(48, 77, 48, 0.2) 0%, transparent 50%);
            animation: heroGlow 8s ease-in-out infinite;
        }

        @keyframes heroGlow {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.6; }
        }

        .hero-content {
            position: relative;
            z-index: 1;
            animation: fadeInUp 1s ease;
        }

        .hero-title {
            font-size: 4rem;
            font-weight: 900;
            color: white;
            margin-bottom: 25px;
            line-height: 1.2;
            letter-spacing: -2px;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }

        .hero-title .highlight {
            background: linear-gradient(135deg, var(--accent) 0%, white 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            font-size: 1.5rem;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 40px;
            font-weight: 500;
        }

        .hero-buttons {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
        }

        .hero-btn {
            padding: 18px 45px;
            border-radius: 50px;
            font-weight: 700;
            font-size: 1.1rem;
            text-decoration: none;
            transition: all 0.4s cubic-bezier(0.68, -0.55, 0.27, 1.55);
            display: inline-flex;
            align-items: center;
            gap: 12px;
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .btn-primary-hero {
            background: white;
            color: var(--primary);
        }

        .btn-primary-hero:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(255, 255, 255, 0.3);
        }

        .btn-secondary-hero {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            color: white;
            border: 2px solid rgba(255, 255, 255, 0.3);
        }

        .btn-secondary-hero:hover {
            background: rgba(255, 255, 255, 0.2);
            border-color: white;
            transform: translateY(-5px);
        }

        .hero-image img {
            width: 100%;
            max-width: 600px;
            filter: drop-shadow(0 20px 60px rgba(0, 0, 0, 0.3));
            animation: floatImage 6s ease-in-out infinite;
        }

        @keyframes floatImage {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        /* Features Cards - Fusion */
        .features-section {
            padding: 100px 0;
            background: linear-gradient(135deg, var(--light) 0%, white 100%);
        }

        .section-header {
            text-align: center;
            margin-bottom: 70px;
        }

        .section-subtitle {
            color: var(--primary);
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-size: 0.95rem;
            margin-bottom: 15px;
        }

        .section-title {
            font-size: 3rem;
            font-weight: 900;
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 20px;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 40px;
        }

        .feature-card {
            background: white;
            border-radius: 25px;
            padding: 45px 35px;
            box-shadow: 0 15px 45px rgba(29, 93, 155, 0.1);
            transition: all 0.4s ease;
            border: 2px solid rgba(117, 194, 246, 0.1);
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 60px rgba(29, 93, 155, 0.15);
        }

        .feature-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            color: white;
            margin-bottom: 25px;
            box-shadow: 0 10px 30px rgba(29, 93, 155, 0.3);
        }

        .feature-card:hover .feature-icon {
            transform: scale(1.1) rotate(5deg);
        }

        .feature-title {
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--dark);
            margin-bottom: 15px;
        }

        .feature-description {
            color: var(--dark);
            line-height: 1.8;
            opacity: 0.8;
        }

        /* Stats Section */
        .stats-section {
            padding: 80px 0;
            background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
            position: relative;
            overflow: hidden;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 40px;
            position: relative;
            z-index: 1;
        }

        .stat-card {
            text-align: center;
            padding: 30px;
        }

        .stat-number {
            font-size: 4rem;
            font-weight: 900;
            color: white;
            margin-bottom: 10px;
            text-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        .stat-label {
            font-size: 1.2rem;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 600;
            text-transform: uppercase;
        }

        /* CTA Section */
        .cta-section {
            padding: 100px 0;
            background: linear-gradient(135deg, var(--light) 0%, white 100%);
        }

        .cta-card {
            background: linear-gradient(135deg, var(--primary) 0%, var(--secondary) 100%);
            border-radius: 30px;
            padding: 80px 60px;
            text-align: center;
            position: relative;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(29, 93, 155, 0.2);
        }

        .cta-title {
            font-size: 3rem;
            font-weight: 900;
            color: white;
            margin-bottom: 20px;
        }

        .cta-description {
            font-size: 1.3rem;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 40px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Card hover effect from Sahty */
        .card-hover {
            transition: all 0.35s ease;
        }
        .card-hover:hover {
            transform: translateY(-12px);
            box-shadow: 0 25px 50px rgba(0,0,0,0.15) !important;
        }

        .icon-circle {
            width: 90px;
            height: 90px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
        }

        /* Animations */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.6s ease, transform 0.6s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }
            .hero-subtitle {
                font-size: 1.2rem;
            }
            .hero-buttons {
                flex-direction: column;
            }
            .hero-btn {
                width: 100%;
                justify-content: center;
            }
            .section-title {
                font-size: 2rem;
            }
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .cta-card {
                padding: 50px 30px;
            }
            .cta-title {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>

    {% include 'partials/navbar.html.twig' %}

    <!-- Hero Section (MEDINOVA style) -->
    <section class="hero-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <div class="hero-content">
                        <h1 class="hero-title">
                            Votre <span class="highlight">Santé</span>,<br>
                            Notre <span class="highlight">Priorité</span>
                        </h1>
                        <p class="hero-subtitle">
                            Prenez soin de votre santé mentale avec des tests rapides, 
                            des résultats clairs et des conseils personnalisés.
                        </p>
                        <div class="hero-buttons">
                            {% if app.user %}
                                {% if is_granted('ROLE_PATIENT') %}
                                    <a href="{{ path('app_rdv_prendre') }}" class="hero-btn btn-primary-hero">
                                        <span>Prendre Rendez-vous</span>
                                        <i class="fas fa-calendar-plus"></i>
                                    </a>
                                    <a href="{{ path('app_rdv_mes_rdv') }}" class="hero-btn btn-secondary-hero">
                                        <span>Mes Rendez-vous</span>
                                        <i class="fas fa-arrow-right"></i>
                                    </a>
                                {% elseif is_granted('ROLE_MEDECIN') %}
                                    <a href="{{ path('app_medecin_rdv_liste') }}" class="hero-btn btn-primary-hero">
                                        <span>Mes Consultations</span>
                                        <i class="fas fa-stethoscope"></i>
                                    </a>
                                    <a href="{{ path('app_fiche_medicale_index') }}" class="hero-btn btn-secondary-hero">
                                        <span>Fiches Patients</span>
                                        <i class="fas fa-folder-open"></i>
                                    </a>
                                {% endif %}
                            {% else %}
                                <a href="{{ path('app_sign') }}" class="hero-btn btn-primary-hero">
                                    <span>Commencer un quiz</span>
                                    <i class="fas fa-arrow-right"></i>
                                </a>
                                <a href="{{ path('app_login') }}" class="hero-btn btn-secondary-hero">
                                    <span>Se Connecter</span>
                                    <i class="fas fa-sign-in-alt"></i>
                                </a>
                            {% endif %}
                        </div>
                        <small class="d-block mt-4 opacity-75">
                            Déjà +1 200 personnes ont commencé leur parcours bien-être avec Sahty
                        </small>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="hero-image">
                        <img src="https://cdni.iconscout.com/illustration/premium/thumb/doctor-consultation-illustration-download-in-svg-png-gif-file-formats--medical-checkup-health-care-pack-professionals-illustrations-4632862.png" 
                             alt="Sahty Healthcare">
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section (3 cards - Sahty style) -->
    <section class="py-5 bg-light">
        <div class="container">
            <div class="text-center mb-5">
                <p class="section-subtitle">Comment ça marche</p>
                <h2 class="section-title">Trois étapes simples</h2>
            </div>
            <div class="row g-4 text-center">
                <div class="col-lg-4 col-md-6">
                    <div class="card h-100 border-0 shadow-lg card-hover">
                        <div class="card-body p-5">
                            <div class="icon-circle bg-primary text-white">
                                <i class="bi bi-journal-text display-4"></i>
                            </div>
                            <h4 class="fw-bold mb-3">Évaluation rapide</h4>
                            <p class="text-muted">
                                Répondez en 5 à 10 minutes à des questions simples et valides scientifiquement.
                            </p>
                        </div>
                    </div>
                </div>

                <div class="col-lg-4 col-md-6">
                    <div class="card h-100 border-0 shadow-lg card-hover">
                        <div class="card-body p-5">
                            <div class="icon-circle bg-warning text-dark">
                                <i class="bi bi-lightbulb display-4"></i>
                            </div>
                            <h4 class="fw-bold mb-3">Conseils adaptés</h4>
                            <p class="text-muted">
                                Recevez des recommandations personnalisées selon vos réponses.
                            </p>
                        </div>
                    </div>
                </div>

                <div class="col-lg-4 col-md-6">
                    <div class="card h-100 border-0 shadow-lg card-hover">
                        <div class="card-body p-5">
                            <div class="icon-circle bg-success text-white">
                                <i class="bi bi-chat-dots display-4"></i>
                            </div>
                            <h4 class="fw-bold mb-3">Suivi discret</h4>
                            <p class="text-muted">
                                Historique privé, progression visible, rappels bienveillants.
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Pourquoi Sahty (from Sahty version) -->
    <section class="py-5">
        <div class="container">
            <div class="text-center mb-5">
                <h2 class="section-title">Pourquoi choisir Sahty ?</h2>
                <p class="lead text-muted">Une application pensée pour vous accompagner</p>
            </div>

            <div class="row g-5 align-items-center">
                <div class="col-lg-6">
                    <img src="{{ asset('img/Bien-être.jpg') }}" 
                         class="img-fluid rounded-4 shadow-lg" 
                         alt="Bien-être mental">
                </div>

                <div class="col-lg-6">
                    <ul class="list-unstyled fs-5">
                        <li class="mb-4">
                            <i class="bi bi-check-circle-fill text-success me-3 fs-3"></i>
                            <strong>Anonymat garanti</strong> — aucune information personnelle obligatoire
                        </li>
                        <li class="mb-4">
                            <i class="bi bi-check-circle-fill text-success me-3 fs-3"></i>
                            <strong>Résultats immédiats</strong> — compréhensibles et sans jargon
                        </li>
                        <li class="mb-4">
                            <i class="bi bi-check-circle-fill text-success me-3 fs-3"></i>
                            <strong>Conseils concrets</strong> — exercices, habitudes, quand consulter un professionnel
                        </li>
                        <li class="mb-4">
                            <i class="bi bi-check-circle-fill text-success me-3 fs-3"></i>
                            <strong>Gratuit et sans pub</strong> — pour votre bien-être, pas pour le profit
                        </li>
                    </ul>

                    <a href="{{ path('app_sign') }}" class="btn btn-primary btn-lg mt-4 px-5">
                        Je veux tester maintenant →
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Grid (MEDINOVA style - 6 cards) -->
    <section class="features-section">
        <div class="container">
            <div class="section-header fade-in">
                <p class="section-subtitle">Nos Services</p>
                <h2 class="section-title">Une plateforme complète</h2>
                <p class="section-description">
                    Tous les outils pour gérer votre santé mentale au quotidien
                </p>
            </div>

            <div class="features-grid">
                <div class="feature-card fade-in">
                    <div class="feature-icon">
                        <i class="fas fa-calendar-check"></i>
                    </div>
                    <h3 class="feature-title">Prise de RDV en Ligne</h3>
                    <p class="feature-description">
                        Réservez vos consultations en quelques clics, 24h/24 et 7j/7.
                    </p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">
                        <i class="fas fa-user-md"></i>
                    </div>
                    <h3 class="feature-title">Médecins Qualifiés</h3>
                    <p class="feature-description">
                        Accédez à un réseau de spécialistes en santé mentale.
                    </p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">
                        <i class="fas fa-file-medical"></i>
                    </div>
                    <h3 class="feature-title">Dossier Médical Digital</h3>
                    <p class="feature-description">
                        Consultez votre historique et vos évaluations en sécurité.
                    </p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">
                        <i class="fas fa-bell"></i>
                    </div>
                    <h3 class="feature-title">Rappels Automatiques</h3>
                    <p class="feature-description">
                        Recevez des notifications pour vos rendez-vous.
                    </p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <h3 class="feature-title">Sécurité Garantie</h3>
                    <p class="feature-description">
                        Vos données sont protégées et confidentielles.
                    </p>
                </div>

                <div class="feature-card fade-in">
                    <div class="feature-icon">
                        <i class="fas fa-headset"></i>
                    </div>
                    <h3 class="feature-title">Support 24/7</h3>
                    <p class="feature-description">
                        Notre équipe est disponible pour vous accompagner.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Stats Section (MEDINOVA style) -->
    <section class="stats-section">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-number" data-target="1250">0</div>
                    <div class="stat-label">Utilisateurs</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number" data-target="85">0</div>
                    <div class="stat-label">Professionnels</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number" data-target="15">0</div>
                    <div class="stat-label">Spécialités</div>
                </div>
                <div class="stat-card">
                    <div class="stat-number" data-target="98">0</div>
                    <div class="stat-label">% Satisfaction</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Final CTA Section (merged) -->
    <section class="cta-section">
        <div class="container">
            <div class="cta-card">
                <div class="cta-content">
                    <h2 class="cta-title">Prêt à prendre soin de vous ?</h2>
                    <p class="cta-description">
                        Commencez maintenant. C'est gratuit, rapide et sans engagement.
                    </p>
                    {% if not app.user %}
                        <a href="{{ path('app_sign') }}" class="hero-btn btn-primary-hero">
                            <span>Créer Mon Compte</span>
                            <i class="fas fa-user-plus"></i>
                        </a>
                    {% else %}
                        <a href="{{ path('app_rdv_prendre') }}" class="hero-btn btn-primary-hero">
                            <span>Prendre Rendez-vous</span>
                            <i class="fas fa-calendar-plus"></i>
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </section>

    <!-- Scripts -->
    <script src="{{ asset('js/bootstrap.bundle.min.js') }}"></script>
    <script src="{{ asset('lib/owlcarousel/owl.carousel.min.js') }}"></script>
    <script src="{{ asset('lib/tempusdominus/js/moment.min.js') }}"></script>
    <script src="{{ asset('lib/tempusdominus/js/tempusdominus-bootstrap-4.min.js') }}"></script>

    <script>
        // Intersection Observer pour les animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => {
            observer.observe(el);
        });

        // Compteur animé pour les statistiques
        function animateCounter(element) {
            const target = parseInt(element.getAttribute('data-target'));
            const duration = 2000;
            const increment = target / (duration / 16);
            let current = 0;

            const updateCounter = () => {
                current += increment;
                if (current < target) {
                    element.textContent = Math.floor(current);
                    requestAnimationFrame(updateCounter);
                } else {
                    element.textContent = target + (element.parentElement.querySelector('.stat-label').textContent.includes('%') ? '%' : '+');
                }
            };

            updateCounter();
        }

        const statsObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    const counter = entry.target.querySelector('.stat-number');
                    animateCounter(counter);
                    statsObserver.unobserve(entry.target);
                }
            });
        }, { threshold: 0.5 });

        document.querySelectorAll('.stat-card').forEach(card => {
            statsObserver.observe(card);
        });

        console.log('🏥 Sahty - Page d\'accueil chargée avec succès!');
    </script>

</body>
</html>