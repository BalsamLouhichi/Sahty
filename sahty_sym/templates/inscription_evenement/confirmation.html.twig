<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Confirmation d'inscription</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.0/css/all.min.css" rel="stylesheet">
    
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .confirmation-card {
            background: white;
            border-radius: 1rem;
            padding: 3rem;
            max-width: 600px;
            text-align: center;
            box-shadow: 0 20px 60px rgba(0,0,0,0.1);
        }
        .success-icon {
            background: #198754;
            width: 80px;
            height: 80px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 2rem;
        }
        .confirmation-details {
            background: #f8f9fa;
            border-radius: 0.75rem;
            padding: 1.5rem;
            margin: 2rem 0;
        }
        .detail-item {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.75rem;
            padding-bottom: 0.75rem;
            border-bottom: 1px dashed #dee2e6;
        }
        .reference {
            background: #0d6efd;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 2rem;
            font-weight: bold;
            display: inline-block;
            margin-bottom: 1rem;
        }
    </style>
</head>
<body>
    <div class="confirmation-card">
        <div class="success-icon">
            <i class="fas fa-check text-white fa-3x"></i>
        </div>
        
        <h1 class="fw-bold mb-3 text-dark">Inscription confirmée !</h1>
        <p class="text-muted mb-4">
            Votre inscription a été enregistrée avec succès.
        </p>
        
        <div class="reference">
            Référence : {{ inscription.reference ?? 'INS-' ~ date().format('Y') ~ '-' ~ inscription.id }}
        </div>
        
        <div class="confirmation-details">
            <div class="detail-item">
                <span class="text-muted">Événement</span>
                <span class="fw-bold">{{ inscription.evenement.titre }}</span>
            </div>
            <div class="detail-item">
                <span class="text-muted">Date</span>
                <span class="fw-bold">{{ inscription.evenement.dateDebut|date('d/m/Y H:i') }}</span>
            </div>
            <div class="detail-item">
                <span class="text-muted">Participant</span>
                <span class="fw-bold">{{ inscription.nom }} {{ inscription.prenom }}</span>
            </div>
            {% if inscription.montant %}
            <div class="detail-item">
                <span class="text-muted">Montant</span>
                <span class="fw-bold text-success">{{ inscription.montant }} €</span>
            </div>
            {% endif %}
            <div class="detail-item">
                <span class="text-muted">Statut</span>
                <span class="badge {% if inscription.statut == 'paye' %}bg-success{% elseif inscription.statut == 'en_attente' %}bg-warning{% else %}bg-secondary{% endif %}">
                    {{ inscription.statut|replace({'_': ' '})|title }}
                </span>
            </div>
        </div>
        
        <div class="alert alert-info mb-4">
            <i class="fas fa-envelope me-2"></i>
            Un email de confirmation vous a été envoyé.
        </div>
        
        <div class="d-flex gap-2 justify-content-center">
            <a href="{{ path('app_evenement_show', {'id': inscription.evenement.id}) }}" 
               class="btn btn-outline-primary">
                <i class="fas fa-calendar me-2"></i>Voir l'événement
            </a>
            <a href="{{ path('app_home') }}" class="btn btn-primary">
                <i class="fas fa-home me-2"></i>Accueil
            </a>
        </div>
    </div>
</body>
</html>