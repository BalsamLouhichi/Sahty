<!-- templates/partials/header.html.twig -->

<!-- Topbar Start - Version Premium avec design amélioré -->
<div class="container-fluid py-2 border-bottom d-none d-lg-block" style="background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%); color: white;">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6 text-center text-lg-start mb-2 mb-lg-0">
                <div class="d-inline-flex align-items-center gap-3">
                    <a class="text-decoration-none text-white" href="#!" style="font-size: 0.95rem; transition: opacity 0.3s;" onmouseover="this.style.opacity='0.8'" onmouseout="this.style.opacity='1'">
                        <i class="bi bi-telephone me-2" style="color: var(--accent);"></i>+216 70 000 000
                    </a>
                    <span class="text-white opacity-25">|</span>
                    <a class="text-decoration-none text-white" href="#!" style="font-size: 0.95rem; transition: opacity 0.3s;" onmouseover="this.style.opacity='0.8'" onmouseout="this.style.opacity='1'">
                        <i class="bi bi-envelope me-2" style="color: var(--accent);"></i>contact@sahty.tn
                    </a>
                </div>
            </div>
            <div class="col-md-6 text-center text-lg-end">
                <div class="d-inline-flex align-items-center gap-2">
                    <a class="text-white social-icon" href="#!" style="width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,255,255,0.1); transition: all 0.3s;" onmouseover="this.style.background='rgba(255,255,255,0.2)'" onmouseout="this.style.background='rgba(255,255,255,0.1)'">
                        <i class="fab fa-facebook-f" style="font-size: 0.9rem;"></i>
                    </a>
                    <a class="text-white social-icon" href="#!" style="width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,255,255,0.1); transition: all 0.3s;" onmouseover="this.style.background='rgba(255,255,255,0.2)'" onmouseout="this.style.background='rgba(255,255,255,0.1)'">
                        <i class="fab fa-twitter" style="font-size: 0.9rem;"></i>
                    </a>
                    <a class="text-white social-icon" href="#!" style="width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,255,255,0.1); transition: all 0.3s;" onmouseover="this.style.background='rgba(255,255,255,0.2)'" onmouseout="this.style.background='rgba(255,255,255,0.1)'">
                        <i class="fab fa-linkedin-in" style="font-size: 0.9rem;"></i>
                    </a>
                    <a class="text-white social-icon" href="#!" style="width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,255,255,0.1); transition: all 0.3s;" onmouseover="this.style.background='rgba(255,255,255,0.2)'" onmouseout="this.style.background='rgba(255,255,255,0.1)'">
                        <i class="fab fa-instagram" style="font-size: 0.9rem;"></i>
                    </a>
                    <a class="text-white social-icon" href="#!" style="width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; border-radius: 50%; background: rgba(255,255,255,0.1); transition: all 0.3s;" onmouseover="this.style.background='rgba(255,255,255,0.2)'" onmouseout="this.style.background='rgba(255,255,255,0.1)'">
                        <i class="fab fa-youtube" style="font-size: 0.9rem;"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- Topbar End -->

<!-- Navbar Start - Design Premium Amélioré -->
<div class="container-fluid sticky-top bg-white shadow-sm" style="border-bottom: 3px solid transparent; border-image: linear-gradient(90deg, var(--primary), var(--accent)); border-image-slice: 1;">
    <div class="container">
        <nav class="navbar navbar-expand-lg bg-white navbar-light py-2 py-lg-0">
            <a href="{{ path('app_home') }}" class="navbar-brand">
                <div class="d-flex align-items-center">
                    <div class="me-2" style="width: 45px; height: 45px; background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%); border-radius: 12px; display: flex; align-items: center; justify-content: center;">
                        <i class="fa fa-heartbeat text-white" style="font-size: 1.5rem;"></i>
                    </div>
                    <h1 class="m-0" style="font-size: 1.8rem; font-weight: 700; background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent;">
                        Sahty
                    </h1>
                </div>
            </a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" style="padding: 0.5rem 0.75rem;">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <div class="navbar-nav mx-auto py-0">
                    <!-- Accueil (commun) -->
                    <a href="{{ path('app_home') }}" class="nav-item nav-link {{ app.current_route == 'app_home' ? 'active' : '' }}">
                        <i class="fa fa-home me-1"></i>Accueil
                    </a>
                   
                    
                    <!-- MENU ADMIN - GESTION ADMIN COMPLÈTE -->
                    {% if is_granted('ROLE_ADMIN') %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                                <i class="fa fa-tachometer-alt me-1"></i>Administration
                            </a>
                            <div class="dropdown-menu m-0">
                                <a href="{{ path('admin_index') }}" class="dropdown-item">
                                    <i class="fa fa-chart-line me-2"></i>Dashboard
                                </a>
                                <div class="dropdown-divider"></div>
                                
                                <h6 class="dropdown-header">Gestion des laboratoires</h6>
                                <a href="{{ path('admin_laboratoires') }}" class="dropdown-item">
                                    <i class="fa fa-flask me-2"></i>Tous les laboratoires
                                </a>
                                <a href="{{ path('admin_laboratoire_new') }}" class="dropdown-item">
                                    <i class="fa fa-plus-circle me-2"></i>Ajouter un laboratoire
                                </a>
                                
                                <div class="dropdown-divider"></div>
                                <h6 class="dropdown-header">Gestion des événements</h6>
                                <a href="{{ path('evenements_evenement_list') }}" class="dropdown-item">
                                    <i class="fa fa-list me-2"></i>Tous les événements
                                </a>
                                <a href="{{ path('evenements_admin_demandes_evenements') }}" class="dropdown-item">
                                    <i class="fa fa-inbox me-2"></i>Demandes d'événements
                                </a>
                            </div>
                        </div>
                    {% endif %}
                    
                    <!-- MENU RENDEZ-VOUS - POUR MÉDECINS ET PATIENTS UNIQUEMENT -->
                    {% if is_granted('ROLE_MEDECIN') or is_granted('ROLE_PATIENT') %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                                <i class="fa fa-calendar-check me-1"></i>Rendez-vous
                            </a>
                            <div class="dropdown-menu m-0">
                                {% if is_granted('ROLE_MEDECIN') %}
                                    <a href="{{ path('app_medecin_rdv_liste') }}" class="dropdown-item">
                                        <i class="fa fa-calendar-check me-2"></i>Mes Consultations
                                    </a>
                                {% endif %}
                                {% if is_granted('ROLE_PATIENT') %}
                                    <a href="{{ path('app_rdv_mes_rdv') }}" class="dropdown-item">
                                        <i class="fa fa-calendar-check me-2"></i>Mes Rendez-vous
                                    </a>
                                    <a href="{{ path('app_rdv_prendre') }}" class="dropdown-item">
                                        <i class="fa fa-plus-circle me-2"></i>Prendre rendez-vous
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                    {% endif %}
                    
                    <!-- MENU LABORATOIRE - POUR RESPONSABLES LABO ET PATIENTS -->
                    {% if is_granted('ROLE_RESPONSABLE_LABO') or is_granted('ROLE_PATIENT') %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                                <i class="fa fa-flask me-1"></i>Laboratoire
                            </a>
                            <div class="dropdown-menu m-0">
                                {% if is_granted('ROLE_RESPONSABLE_LABO') %}
                                    <a href="{{ path('app_demande_analyse_index') }}" class="dropdown-item">
                                        <i class="fa fa-file-medical me-2"></i>Demandes d'analyses
                                    </a>
                                {% endif %}
                                {% if is_granted('ROLE_PATIENT') %}
                                    <a href="{{ path('app_labo_index') }}" class="dropdown-item">
                                        <i class="fa fa-flask me-2"></i>Liste des laboratoires
                                    </a>
                                    <a href="{{ path('app_demande_analyse_mes_demandes') }}" class="dropdown-item">
                                        <i class="fa fa-file-medical me-2"></i>Mes demandes d'analyses
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                    {% endif %}
                    
                    <!-- MENU PARAPHARMACIE - POUR RESPONSABLE PARA ET PATIENTS -->
                    {% if is_granted('ROLE_RESPONSABLE_PARA') or is_granted('ROLE_PATIENT') %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                                <i class="fa fa-pills me-1"></i>Parapharmacie
                            </a>
                            <div class="dropdown-menu m-0">
                                {% if is_granted('ROLE_RESPONSABLE_PARA') %}
                                    <a href="{{ path('app_parapharmacie_dashboard') }}" class="dropdown-item">
                                        <i class="fa fa-tachometer-alt me-2"></i>Dashboard
                                    </a>
                                    <a href="{{ path('app_parapharmacie_produits') }}" class="dropdown-item">
                                        <i class="fa fa-boxes me-2"></i>Produits
                                    </a>
                                    <a href="{{ path('app_parapharmacie_categories') }}" class="dropdown-item">
                                        <i class="fa fa-tags me-2"></i>Catégories
                                    </a>
                                {% endif %}
                                {% if is_granted('ROLE_PATIENT') %}
                                    <a href="{{ path('app_parapharmacie_produits') }}" class="dropdown-item">
                                        <i class="fa fa-shopping-basket me-2"></i>Nos produits
                                    </a>
                                    <a href="{{ path('app_mes_commandes')|default('#') }}" class="dropdown-item">
                                        <i class="fa fa-box me-2"></i>Mes commandes
                                    </a>
                                    {# <a href="{{ path('app_panier')|default('#') }}" class="dropdown-item">
                                        <i class="fa fa-shopping-cart me-2"></i>Mon panier
                                    </a> #}
                                {% endif %}
                            </div>
                        </div>
                    {% endif %}
                    
                    <!-- MENU ÉVÉNEMENTS - POUR TOUS LES UTILISATEURS CONNECTÉS SAUF ADMIN -->
                    {% if app.user and not is_granted('ROLE_ADMIN') %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                                <i class="fas fa-calendar-alt me-1"></i>Événements
                            </a>
                            <div class="dropdown-menu m-0">
                                <a href="{{ path('evenements_client_events') }}" class="dropdown-item {{ app.current_route starts with 'evenements_client' ? 'active' : '' }}">
                                    <i class="fas fa-calendar-check me-2"></i>Mes Événements
                                </a>
                                <a href="{{ path('evenements_client_all_events') }}" class="dropdown-item {{ app.current_route == 'evenements_client_all_events' ? 'active' : '' }}">
                                    <i class="fas fa-list me-2"></i>Tous les événements
                                </a>
                                <a href="{{ path('evenements_client_mes_inscriptions') }}" class="dropdown-item">
                                    <i class="fas fa-ticket-alt me-2"></i>Mes inscriptions
                                </a>
                                
                                {# NE PAS AFFICHER "Demander un événement" POUR LES PATIENTS #}
                                {% if not is_granted('ROLE_PATIENT') %}
                                    <div class="dropdown-divider"></div>
                                    <a href="{{ path('evenements_client_demande_evenement') }}" class="dropdown-item">
                                        <i class="fa fa-paper-plane me-2"></i>Demander un événement
                                    </a>
                                {% endif %}
                            </div>
                        </div>
                    {% endif %}
                    
                    <!-- MENU MÉDECIN - ESPACE MÉDECIN (complémentaire) -->
                    {% if is_granted('ROLE_MEDECIN') %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                                <i class="fa fa-stethoscope me-1"></i>Espace Médecin
                            </a>
                            <div class="dropdown-menu m-0">
                                <a href="{{ path('app_fiche_medicale_index') }}" class="dropdown-item">
                                    <i class="fa fa-notes-medical me-2"></i>Fiches Patients
                                </a>
                                <a href="{{ path('app_profile') }}" class="dropdown-item">
                                    <i class="fa fa-user-md me-2"></i>Mon Profil
                                </a>
                            </div>
                        </div>
                    {% endif %}
                    
                    <!-- MENU PATIENT - ESPACE PATIENT (complémentaire) -->
                    {% if is_granted('ROLE_PATIENT') and not is_granted('ROLE_MEDECIN') and not is_granted('ROLE_RESPONSABLE_LABO') and not is_granted('ROLE_RESPONSABLE_PARA') %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">
                                <i class="fa fa-user me-1"></i>Mon Espace
                            </a>
                            <div class="dropdown-menu m-0">
                                <a href="{{ path('app_fiche_medicale_index') }}" class="dropdown-item">
                                    <i class="fa fa-file-medical me-2"></i>Mes Fiches Médicales
                                </a>
                                <a href="{{ path('app_profile') }}" class="dropdown-item">
                                    <i class="fa fa-user-circle me-2"></i>Mon Profil
                                </a>
                            </div>
                        </div>
                    {% endif %}
                </div>
                
                <!-- User Menu - Design Premium Amélioré -->
                <div class="d-flex align-items-center ms-lg-3 mt-3 mt-lg-0">
                    {% if app.user %}
                        <div class="nav-item dropdown">
                            <a href="#" class="nav-link dropdown-toggle d-flex align-items-center gap-2 px-3 py-2" data-bs-toggle="dropdown" style="border-radius: 50px; background: rgba(29, 93, 155, 0.05); border: 1px solid rgba(29, 93, 155, 0.1);">
                                {% if app.user.photoProfil %}
                                    <img src="{{ asset(app.user.photoProfil) }}" alt="Profil" style="width: 38px; height: 38px; border-radius: 50%; object-fit: cover; border: 2px solid var(--primary);">
                                {% else %}
                                    <span class="d-inline-flex align-items-center justify-content-center" style="width: 38px; height: 38px; border-radius: 50%; background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%); color: white; border: 2px solid white; box-shadow: 0 2px 10px rgba(29,93,155,0.2);">
                                        <i class="fa fa-user" style="font-size: 1rem;"></i>
                                    </span>
                                {% endif %}
                                <span class="fw-semibold" style="color: var(--primary); font-size: 0.95rem;">{{ app.user.prenom }} {{ app.user.nom|slice(0, 1) }}.</span>
                                <i class="fa fa-chevron-down" style="font-size: 0.8rem; color: var(--primary); opacity: 0.7;"></i>
                            </a>
                            <div class="dropdown-menu dropdown-menu-end m-0" style="border-radius: 12px; border: none; box-shadow: 0 10px 40px rgba(0,0,0,0.15); padding: 0.5rem;">
                                <div class="px-3 py-2 mb-2" style="border-bottom: 1px solid #eee;">
                                    <small class="text-muted">Connecté en tant que</small>
                                    <div class="fw-bold">{{ app.user.email }}</div>
                                </div>
                                <a href="{{ path('app_profile') }}" class="dropdown-item" style="border-radius: 8px; padding: 0.6rem 1rem;">
                                    <i class="fa fa-user-circle me-2" style="color: var(--primary);"></i>Mon Profil
                                </a>
                                
                                {# Lien rapide vers les événements pour tous les clients SAUF PATIENTS #}
                                {% if app.user and not is_granted('ROLE_ADMIN') %}
                                    {# Vérifier que l'utilisateur n'est PAS un patient #}
                                    {% if not is_granted('ROLE_PATIENT') %}
                                        <a href="{{ path('evenements_client_demande_evenement') }}" class="dropdown-item" style="border-radius: 8px; padding: 0.6rem 1rem;">
                                            <i class="fa fa-paper-plane me-2" style="color: var(--primary);"></i>Demander un événement
                                        </a>
                                    {% endif %}
                                {% endif %}
                                
                                <hr class="dropdown-divider">
                                <a href="{{ path('app_logout') }}" class="dropdown-item text-danger" style="border-radius: 8px; padding: 0.6rem 1rem;">
                                    <i class="fa fa-sign-out-alt me-2"></i>Déconnexion
                                </a>
                            </div>
                        </div>
                    {% else %}
                        <div class="d-flex gap-2">
                            <a href="{{ path('app_signup') }}" class="btn btn-outline-primary px-4" style="border-radius: 50px; font-weight: 600; border-width: 2px;">
                                <i class="fa fa-user-plus me-2"></i>S'inscrire
                            </a>
                            <a href="{{ path('app_login') }}" class="btn btn-primary px-4" style="border-radius: 50px; font-weight: 600; background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%); border: none;">
                                <i class="fa fa-sign-in-alt me-2"></i>Se connecter
                            </a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </nav>
    </div>
</div>
<!-- Navbar End -->

<style>
/* Styles améliorés pour le header */
:root {
    --primary: #1D5D9B;
    --primary-dark: #163A5F;
    --accent: #75C2F6;
    --light-bg: #f8faff;
}

/* Navbar styles */
.navbar {
    transition: all 0.3s ease;
}

.navbar-nav {
    gap: 0.25rem;
}

.navbar-nav .nav-link {
    font-weight: 600;
    padding: 0.7rem 1.2rem !important;
    color: #2c3e50;
    transition: all 0.3s ease;
    border-radius: 50px;
    font-size: 0.95rem;
}

.navbar-nav .nav-link:hover {
    color: var(--primary) !important;
    background: rgba(29, 93, 155, 0.05);
    transform: translateY(-1px);
}

.navbar-nav .nav-link.active {
    color: var(--primary) !important;
    background: rgba(29, 93, 155, 0.08);
    position: relative;
}

.navbar-nav .nav-link i {
    opacity: 0.8;
    transition: all 0.3s ease;
}

.navbar-nav .nav-link:hover i {
    opacity: 1;
    transform: scale(1.1);
}

/* Dropdown styles */
.dropdown-menu {
    border: none;
    box-shadow: 0 15px 40px rgba(0,0,0,0.12);
    border-radius: 16px;
    overflow: hidden;
    animation: slideDown 0.3s ease;
    max-height: 80vh;
    overflow-y: auto;
    padding: 0.75rem;
    border: 1px solid rgba(29, 93, 155, 0.1);
    min-width: 260px;
}

@keyframes slideDown {
    from {
        opacity: 0;
        transform: translateY(-10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

.dropdown-item {
    padding: 0.7rem 1.2rem;
    font-weight: 500;
    transition: all 0.3s ease;
    border-radius: 10px;
    margin-bottom: 2px;
    font-size: 0.95rem;
}

.dropdown-item:hover {
    background: rgba(29, 93, 155, 0.08);
    color: var(--primary) !important;
    transform: translateX(5px);
}

.dropdown-item i {
    width: 22px;
    color: var(--primary);
    font-size: 1rem;
}

.dropdown-header {
    color: var(--primary);
    font-weight: 700;
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    padding: 0.75rem 1.2rem 0.25rem;
}

.dropdown-divider {
    margin: 0.5rem 0;
    opacity: 0.5;
}

/* Button styles */
.btn-outline-primary {
    border: 2px solid var(--primary);
    color: var(--primary);
    font-weight: 600;
    transition: all 0.3s ease;
    padding: 0.5rem 1.5rem;
}

.btn-outline-primary:hover {
    background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
    border-color: transparent;
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(29, 93, 155, 0.3);
    color: white;
}

.btn-primary {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    border: none;
    font-weight: 600;
    transition: all 0.3s ease;
    padding: 0.5rem 1.5rem;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(29, 93, 155, 0.4);
}

/* Social icons */
.social-icon {
    transition: all 0.3s ease;
}

.social-icon:hover {
    transform: translateY(-2px);
    background: rgba(255,255,255,0.2) !important;
}

/* Responsive adjustments */
@media (max-width: 991.98px) {
    .navbar-nav {
        padding: 1rem 0;
    }
    
    .navbar-nav .nav-link {
        border-radius: 8px;
        padding: 0.7rem 1rem !important;
    }
    
    .dropdown-menu {
        box-shadow: none;
        border: 1px solid rgba(29, 93, 155, 0.1);
        margin: 0.5rem 0;
    }
}
</style>

<script>
// Active link highlighting based on current route
document.addEventListener('DOMContentLoaded', function() {
    const currentPath = window.location.pathname;
    const navLinks = document.querySelectorAll('.navbar-nav .nav-link');
    
    navLinks.forEach(link => {
        if (link.getAttribute('href') === currentPath) {
            link.classList.add('active');
        }
    });
});

// Navbar scroll effect
window.addEventListener('scroll', function() {
    const navbar = document.querySelector('.navbar');
    if (window.scrollY > 50) {
        navbar.classList.add('navbar-scrolled');
    } else {
        navbar.classList.remove('navbar-scrolled');
    }
});
</script>
