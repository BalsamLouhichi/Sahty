<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Groupes Cibles</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {# Google Fonts #}
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">

    {# FontAwesome #}
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.0/css/all.min.css" rel="stylesheet">

    {# Bootstrap #}
    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">

    {# Main CSS #}
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">

    <style>
        /* Matching styles from event page */
        .container-fluid.py-5 {
            background-color: #f8f9fa;
            min-height: 100vh;
            padding-top: 2rem;
            padding-bottom: 2rem;
        }
        
        .display-6 {
            color: #212529;
        }
        
        .card {
            border: none;
            box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075);
        }
        
        .btn-primary, .btn-secondary {
            padding: 0.75rem 2rem;
            font-weight: 600;
            border-radius: 0.5rem;
        }
        
        /* Table specific styles */
        .table {
            margin-bottom: 0;
        }
        
        .table thead th {
            font-weight: 600;
            color: #495057;
            border-bottom-width: 2px;
            padding-top: 1rem;
            padding-bottom: 1rem;
        }
        
        .table tbody tr {
            transition: all 0.2s;
        }
        
        .table tbody tr:hover {
            background-color: rgba(13, 110, 253, 0.02);
        }
        
        .table tbody td {
            padding: 1rem 0.75rem;
            vertical-align: middle;
            border-color: #e9ecef;
        }
        
        /* Action buttons */
        .action-buttons {
            white-space: nowrap;
        }
        
        .action-buttons .btn {
            width: 34px;
            height: 34px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 6px;
            margin: 0 2px;
            transition: all 0.2s;
        }
        
        .action-buttons .btn-edit {
            background-color: rgba(13, 110, 253, 0.1);
            color: #0d6efd;
            border: 1px solid rgba(13, 110, 253, 0.2);
        }
        
        .action-buttons .btn-edit:hover {
            background-color: #0d6efd;
            color: white;
            border-color: #0d6efd;
        }
        
        .action-buttons .btn-delete {
            background-color: rgba(220, 53, 69, 0.1);
            color: #dc3545;
            border: 1px solid rgba(220, 53, 69, 0.2);
        }
        
        .action-buttons .btn-delete:hover {
            background-color: #dc3545;
            color: white;
            border-color: #dc3545;
        }
        
        /* Badge for ID */
        .id-badge {
            background-color: #f8f9fa;
            color: #6c757d;
            font-weight: 500;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.85rem;
        }
        
        /* Empty state */
        .empty-state {
            padding: 3rem 1rem;
            text-align: center;
        }
        
        .empty-state-icon {
            font-size: 3rem;
            color: #dee2e6;
            margin-bottom: 1rem;
        }
        
        /* Card header */
        .card-header {
            background-color: #f8f9fa;
            border-bottom: 1px solid rgba(0,0,0,.125);
            padding: 1.25rem 1.5rem;
        }
        
        .card-header h5 {
            margin-bottom: 0;
            font-weight: 600;
        }
        
        /* Responsive adjustments */
        @media (max-width: 768px) {
            .table-responsive {
                border-radius: 0.5rem;
                border: 1px solid #dee2e6;
            }
            
            .action-buttons {
                display: flex;
                gap: 0.5rem;
            }
            
            .action-buttons .btn {
                flex: 1;
                min-width: 40px;
            }
        }
    </style>
</head>

<body>

{# Header #}
{% include 'partials/header.html.twig' %}

<!-- Main Content -->
<div class="container-fluid py-5">
    <div class="container">

        <!-- Header -->
        <div class="row mb-5">
            <div class="col-md-8">
                <h1 class="display-6 fw-bold mb-3 text-dark">
                    <i class="fas fa-users text-primary me-2"></i>
                    Groupes Cibles
                </h1>
                <p class="text-muted mb-0">Gérez les groupes cibles pour vos événements</p>
            </div>
            <div class="col-md-4 d-flex align-items-center justify-content-end">
                <a href="{{ path('evenements_evenement_add') }}" class="btn btn-outline-secondary me-2">
                    <i class="fas fa-arrow-left me-2"></i>Retour aux événements
                </a>
                <a href="{{ path('groupe_cible_new') }}" class="btn btn-primary">
                    <i class="fas fa-plus me-1"></i> Nouveau Groupe
                </a>
            </div>
        </div>

        <!-- Content Card -->
        <div class="card">
            <div class="card-header">
                <div class="d-flex justify-content-between align-items-center">
                    <h5 class="mb-0">Liste des groupes</h5>
                    <span class="badge bg-light text-dark">{{ groupes|length }} groupe{{ groupes|length > 1 ? 's' : '' }}</span>
                </div>
            </div>
            
            <div class="card-body">
                {% if groupes is empty %}
                <div class="empty-state">
                    <div class="empty-state-icon">
                        <i class="fas fa-users-slash"></i>
                    </div>
                    <h4 class="text-muted mb-3">Aucun groupe cible trouvé</h4>
                    <p class="text-muted mb-4">Commencez par créer votre premier groupe cible pour vos événements.</p>
                    <a href="{{ path('groupe_cible_new') }}" class="btn btn-primary px-4">
                        <i class="fas fa-plus me-1"></i> Créer un groupe
                    </a>
                </div>
                {% else %}
                <div class="table-responsive">
                    <table class="table table-hover align-middle">
                        <thead>
                            <tr>
                                <th width="100">ID</th>
                                <th>Nom</th>
                                {% if groupes[0].description is defined %}
                                    <th>Description</th>
                                {% endif %}
                                <th width="120" class="text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for groupe in groupes %}
                            <tr>
                                <td>
                                    <span class="id-badge">#{{ groupe.id }}</span>
                                </td>
                                <td>
                                    <strong>{{ groupe.nom }}</strong>
                                </td>
                                {% if groupe.description is defined %}
                                    <td>
                                        {% if groupe.description %}
                                            <span class="text-muted">
                                                {{ groupe.description|length > 80 ? groupe.description|slice(0, 80) ~ '...' : groupe.description }}
                                            </span>
                                        {% else %}
                                            <span class="text-muted fst-italic">Aucune description</span>
                                        {% endif %}
                                    </td>
                                {% endif %}
                                <td class="text-end action-buttons">
                                    <a href="{{ path('groupe_cible_edit', {'id': groupe.id}) }}" 
                                       class="btn btn-sm btn-edit" 
                                       title="Modifier">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form method="post" 
                                          action="{{ path('groupe_cible_delete', {'id': groupe.id}) }}" 
                                          class="d-inline"
                                          onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer le groupe « {{ groupe.nom }} » ? Cette action est irréversible.');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupe.id) }}">
                                        <button type="submit" 
                                                class="btn btn-sm btn-delete" 
                                                title="Supprimer">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
                
                <!-- Footer with stats -->
                <div class="d-flex justify-content-between align-items-center mt-4 pt-3 border-top">
                    <small class="text-muted">
                        <i class="fas fa-info-circle me-1"></i>
                        Les groupes cibles sont utilisés pour segmenter les participants de vos événements.
                    </small>
                    <a href="{{ path('groupe_cible_new') }}" class="btn btn-outline-primary btn-sm">
                        <i class="fas fa-plus me-1"></i> Ajouter un groupe
                    </a>
                </div>
                {% endif %}
            </div>
        </div>

    </div>
</div>

<!-- Footer -->
{% include 'partials/footer.html.twig' %}

<!-- Scripts -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Prevent delete form from bubbling to parent forms
    document.querySelectorAll('.btn-delete').forEach(button => {
        button.addEventListener('click', function(e) {
            // Check if we're inside a form
            if (this.closest('form')) {
                e.stopPropagation();
            }
        });
    });
    
    // Add row hover effect for better UX
    const rows = document.querySelectorAll('tbody tr');
    rows.forEach(row => {
        row.addEventListener('mouseenter', function() {
            this.style.backgroundColor = 'rgba(13, 110, 253, 0.02)';
        });
        
        row.addEventListener('mouseleave', function() {
            this.style.backgroundColor = '';
        });
    });
});
</script>

</body>
</html>